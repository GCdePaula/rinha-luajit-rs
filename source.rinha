let sum_cps = fn (n, cont) => {
    if (n == 0) {
      cont(0)
    } else {
      sum_cps(n-1, fn(x) => {cont(x + n)})
    }
};


let sum = fn (n) => {
  sum_cps(n, fn(n) => {n})
};


print(sum(10000000))
